# Implementation Plan: Report Evaluator 三層升級（L1 保留 / L2 Rule-based / L3 LLM-as-Judge）

> Generated by /design on 2026-03-21
> Plan file: plans/active/report-evaluator-l2-l3.md
> Status: IMPLEMENTED (Step 11 閾值待主 repo 跑 43 份報告後設定)
> Worktree: ~/Documents/seo-knowledge-insight-report-eval
> Branch: feat/report-evaluator-l2-l3

## Overview

現有 report-evaluator.ts 的 5 個指標呈二元分布（43 份報告中 31 份滿分 0.97-1.0、6 份零分 0.2），無法鑑別品質差異。升級為三層：L1 保留（結構護欄）、L2 新增 6 個 rule-based 連續指標、L3 新增 Claude Code as LLM-as-Judge 3 維度。新 `composite_v2` 取代飽和的 `overall`，使分布從二元變為連續，支撐 autoresearch 報告品質優化循環。

**重要：全程不需要任何外部 API key。L3 使用 Claude Code 本身作為 Judge（和 /evaluate-qa-local、/evaluate-meeting-prep-quality 同模式）。**

## ECC Resources

| Resource | Type | Usage |
|----------|------|-------|
| tdd-guide | agent | Phase 1: TDD 先寫測試 |
| typescript-reviewer | agent | Phase 4: TS 程式碼品質 |
| python-reviewer | agent | Phase 5: Python 版品質 |
| code-reviewer | agent | Phase 7: 整體審查 |

## Industry & Standards Reference

| 技術決策 | 參照依據 | 類型 | 來源 |
|----------|---------|------|------|
| LLM-as-Judge 評分 | G-Eval (Liu et al., 2023) | 學術研究 | NeurIPS 2023 |
| Structured output | Anthropic tool_use | 業界標準 | Anthropic Docs |
| temperature=0 for eval | RAGAS / DeepEval 慣例 | 最佳實踐 | RAGAS framework |
| 分層評估（L1/L2/L3） | 專案 meeting-prep-eval 慣例 | 專案慣例 | plans/active/meeting-prep-eval-l3-l4.md |

## 問題分析（實測數據）

```
43 份報告 overall 分布：
  0.200 ████░░░░░░░░░░░░░░░░ 6 份（空/不符格式）
  0.400 █░░░░░░░░░░░░░░░░░░░ 1 份
  0.600 █░░░░░░░░░░░░░░░░░░░ 1 份
  0.905 ██░░░░░░░░░░░░░░░░░░ 2 份
  0.971 ████████████████████ 25 份（飽和）
  1.000 ████████░░░░░░░░░░░░ 8 份（滿分）

→ 31/43 = 72% 在 0.97-1.0，無鑑別力
```

## L2 新指標設計（6 個 rule-based）

| 指標 | 量什麼 | 偵測方式 | 預期鑑別範圍 |
|------|--------|---------|-------------|
| `cross_metric_reasoning` | 跨指標因果推理 | 因果連接詞 + 同段 ≥2 個指標名 | 0.0–1.0 |
| `action_specificity` | 行動建議具體度 | 具體動作詞 vs 模糊詞比例 | 0.2–0.9 |
| `data_evidence_ratio` | 數據引用密度 | 百分比模式 + 大數字計數 / 20 | 0.3–1.0 |
| `citation_integration` | 引用整合度 | body 內 `[N]` vs 尾部堆積 | 0.0–1.0 |
| `quadrant_judgment` | 流量象限判斷 | 象限關鍵字 + 解釋 | 0.0/0.5/1.0 |
| `section_depth_variance` | Section 字數均衡度 | 1 - (std / mean) | 0.4–0.9 |

## L3 LLM-as-Judge 設計（Claude Code as Judge，3 維度）

**不使用外部 API**。Claude Code 本身讀取報告內容，依 rubric 評分（和 `/evaluate-meeting-prep-quality` 同模式）。

實作方式：
- 新增 `/evaluate-report-quality` slash command（`.claude/commands/evaluate-report-quality.md`）
- 或新增 `evals/eval_report_llm.py`（Claude Code 讀報告 → 輸出 JSON 分數）
- Makefile: `make evaluate-report-llm`

| 維度 | 1 分 | 3 分 | 5 分 |
|------|------|------|------|
| `reasoning_depth` | 只列數字 | 有跨指標比較但無因果 | 多步因果 + 研究佐證 |
| `actionability` | 「注意 X」 | 「檢查 X 的 Y」 | 「在 GSC 的 Y 報表篩選 Z 條件」 |
| `insight_originality` | 複述數字 | 有基本解讀 | 發現非顯而易見的關聯 |

## Composite V2 公式

```
有 L3：
composite_v2 = L1_overall×0.20 + cross_metric×0.10 + action_specificity×0.10
             + data_evidence×0.08 + citation_integration×0.07
             + quadrant_judgment×0.05 + section_depth_var×0.05
             + reasoning_depth/5×0.15 + actionability/5×0.10
             + insight_originality/5×0.10

無 L3（auto fallback）：
composite_v2 = L1_overall×0.30 + cross_metric×0.15 + action_specificity×0.15
             + data_evidence×0.12 + citation_integration×0.10
             + quadrant_judgment×0.10 + section_depth_var×0.08
```

## Implementation Steps

### Phase 1: TDD 先寫測試

- [x] Step 1: `api/tests/services/report-evaluator-l2.test.ts`（新建）
  - 6 個 L2 function 各 3-5 cases，含邊界條件（34 tests）
  - 好報告 fixture + 差報告 fixture
- [x] Step 2: L3 rubric 設計文件
  - 定義 3 個維度的 1-5 分 rubric（供 Claude Code 評分用）
  - 寫入 `.claude/commands/evaluate-report-quality.md`

### Phase 2: L2 實作（TS）

- [x] Step 3: `api/src/services/report-evaluator-l2.ts`（新建，~200 行）
  - 6 個 pure functions + `evaluateReportL2()` 聚合
  - `ReportEvalL2Result` interface
  - SEO 指標詞彙常數表（for cross_metric_reasoning）
- [x] Step 4: 更新 `report-evaluator.ts` 的 `ReportEvalResult` interface
  - `ReportEvalV2Result` + `ReportEvalL3Scores` types + `composite_v2` + `l3_skipped`
  - 原 `evaluateReport()` 保持 sync 僅 L1（向下相容）

### Phase 3: L3 實作（Claude Code slash command）

- [x] Step 5: `.claude/commands/evaluate-report-quality.md`（新建）
  - Claude Code 讀指定報告 → 依 rubric 評 3 維度 → 輸出 JSON
  - 和 `/evaluate-meeting-prep-quality` 同模式
  - 輸出寫入 `output/eval_report_quality_YYYYMMDD.json`
- [x] Step 6: `report-evaluator.ts` 新增 `evaluateReportV2()` + `computeCompositeV2()`
  - sync，呼叫 L1 + L2 + composite_v2（不含 L3，L3 由 Claude Code 獨立跑）
  - L3 分數由外部注入（API route 或 eval 腳本合併）

### Phase 4: TS 測試 + routes 整合

- [x] Step 7: 更新 `report-evaluator.test.ts`（+8 tests）
  - 回歸驗證 + composite_v2 公式 + weight sum 驗證
- [x] Step 8: 更新 `routes/reports.ts` eval block
  - `evaluateReport()` → `evaluateReportV2()` + L2 scoreEvents + `composite_v2`

### Phase 5: Python 同步（可和 Phase 4 並行）

- [x] Step 9: 更新 `scripts/_eval_report.py`
  - L2 pure functions（對齊 TS 邏輯）
  - composite_v2（L1+L2）+ Laminar push
  - 不含 L3（L3 由 `/evaluate-report-quality` slash command 執行）

### Phase 6: 配置

- [ ] Step 11: `eval/eval_thresholds.json` 新增 `report_quality_v2` 組
  - 先跑 43 份報告確認分數分布，再設閾值（**需在主 repo 執行**）
- [x] Step 12: `Makefile` 新增 `evaluate-report`
- [x] Step 13: `.env.example` — 不需更新（無新 env var）

### Phase 7: 驗證 + 文件

- [x] Step 14: 驗證測試矩陣
  - L1 regression: 26/26 PASS
  - L2: 34/34 PASS
  - V2/composite: 8/8 PASS
  - TypeScript type check: exit 0
  - coverage: report-evaluator.ts 100% / report-evaluator-l2.ts 98%+
- [x] Step 15: 文件更新（CLAUDE.md 新增 `/evaluate-report-quality` + `make evaluate-report`）

## Architecture — 檔案結構

```
api/src/services/
├── report-evaluator.ts        # 主入口（擴充 interface + evaluateReportV2）
└── report-evaluator-l2.ts     # L2 pure functions（新建，~200 行）

api/tests/services/
├── report-evaluator.test.ts   # 既有 tests + 新增
└── report-evaluator-l2.test.ts # 新建（~35 tests）

.claude/commands/
└── evaluate-report-quality.md # L3 Claude Code as Judge（新建）

scripts/
└── _eval_report.py            # 擴充 L2 + composite_v2
```

**注意：不建立 `report-evaluator-l3.ts`。L3 由 Claude Code 本身執行，不在 API server 內。**

## Risks & Mitigations

| 風險 | 對策 |
|------|------|
| `cross_metric_reasoning` 中文正則 false positive | 嚴格：因果詞 + 100 字內 ≥2 個不同指標名 |
| L3 timeout 影響 API response | 30s AbortSignal.timeout()，超時 return null |
| `@anthropic-ai/sdk` 未安裝 | Phase 3 前 `pnpm add @anthropic-ai/sdk`（若無） |
| 閾值設太高 CI gate 誤 fail | 先跑 43 份 dry-run 確認分布再設閾值 |
| Python/TS 邏輯不一致 | Phase 5 完成後交叉驗證：同一份報告兩版分數誤差 < 0.05 |

## Acceptance Criteria

- [ ] 既有 19 個 L1 tests 零退化
- [ ] L2 tests 全 PASS（~35 個）
- [ ] L3 mock tests 全 PASS（~10 個）
- [ ] 高品質報告 composite_v2 > 0.6，舊格式/空報告 < 0.3（分布連續化）
- [ ] `make evaluate-report` 成功輸出 composite_v2
- [ ] `make evaluate-report-full` 有 key → L3；無 key → SKIPPED + exit 0
- [ ] coverage 80%+

## Phase 依賴圖

```
Phase 1（TDD）→ Phase 2（L2）→ Phase 3（L3）→ Phase 4（routes）
                                    ↓
                              Phase 5（Python，可平行）
Phase 4 + Phase 5 → Phase 6（config）→ Phase 7（驗證）
```
